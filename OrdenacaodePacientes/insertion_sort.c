#include "insertion_sort.h"
#include <string.h>

void insertionSort(Paciente v[], int esq, int dir) {
    int i, j;
    Paciente aux;

    for (i = esq + 1; i <= dir; i++) {
        aux = v[i];
        j = i - 1;

while()
    }
}
